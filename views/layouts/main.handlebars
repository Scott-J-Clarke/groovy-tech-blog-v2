<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dose of Daily Tech</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    {{!-- <link rel="stylesheet" href="/public/css/style.css"> Will stylesheet be necessary if using Bulma? --}}
</head>

<div class="wrapper">
    <header>
        <h1 class="has-text-black px-3 py-3 has-text-weight-bold">Dose of Daily Tech</h1>
        
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" class="navbar-item is-hoverable pt-1">Homepage</a>
            <a href="/dashboard" class="navbar-item is-hoverable pt-1">Dashboard</a>
            <a href="/dashboard/create" class="navbar-item is-hoverable pt-1 mr-4">Create Post</a>
            <button id="logout" class="button is-danger">Logout</button>
            {{else}}
            <a href="/" class="navbar-item is-hoverable pt-1">Homepage</a>
            <a href="/login" class="navbar-item is-hoverable pt-1">Dashboard</a>
            <a href="/login" class="navbar-item is-hoverable pt-1">Create Post</a>
            <a href="/login" class="navbar-item is-hoverable pt-1">Login</a>
            {{/if}}
        </nav>
     
    </header>

    <main>
            {{{ body }}}
    </main>

    {{!-- Have no footer - can add this later if there is time. --}}
</div>

{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}

</html>
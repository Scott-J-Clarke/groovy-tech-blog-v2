<div class="mt-5">

    <h2 class="title pb-4">Dashboard: Your Posts</h2><hr>
{{#if posts.length}}
</div>

{{#each posts as |post|}}
{{> post-info post}}

{{!-- Button below is for editing posts: --}}
{{post.id}} {{!-- {{post.id}} is a variable/placeholder that represents the 'id' property of an object called 'post' --}} 
<a href="dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-warning">Edit Post</button></a>
</div>

{{/each}}

{{else}}
<h6 class="pb-4 has-centered-text is-italic">You do not have any posts.</h6>
{{/if}}
